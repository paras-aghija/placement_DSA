#include<bits/stdc++.h>
#define FIO ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0)
using namespace std;

/*

	(a + b) % m = ( (a % m) + (b % m) ) % m
	(a - b) % m = ( (a % m) - (b % m) + m ) % m
	(a * b) % m = ( (a % m) * (b % m) ) % m
	(a / b) % m = ( (a % m) * (b^-1 % m) ) % m

	b^-1 => Multiplicative modulo inverse
*/

int main() {

	FIO;


	return 0;
}